package com.lhy.ae2utility.api.pattern;

/** Result of handing a request to the server-side encoding service. */
public enum PatternEncodingResult {
    ACCEPTED,
    INVALID_REQUEST,
    WRONG_SIDE
}